import styled from '@emotion/styled'
import { Box } from '@mui/material'

export const PostsContainer = styled(Box)({
    display: 'flex',
    flexDirection: 'row',
    gap: '1rem',
    height: '100%',
    overflowY: 'hidden',
})

